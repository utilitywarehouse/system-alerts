apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: prometheus
spec:
  template:
    spec:
      volumes:
        - name: rules-projected
          projected:
            sources:
              - configMap: { name: alert-templates-common }
