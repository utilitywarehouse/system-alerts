apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: prometheus
spec:
  template:
    spec:
      initContainers:
        - name: rule-watcher
          volumeMounts:
            - name: rule-templates-cumulus
              mountPath: /var/prometheus/rule-templates/cumulus
      volumes:
        - name: rule-templates-cumulus
          configMap:
            name: alert-templates-cumulus
